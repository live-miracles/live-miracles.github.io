{
    "scripts": {
        "build": "node scripts/build.js && tailwindcss -i ./ui/styles.css -o ./dist/assets/styles.css --minify",
        "dev": "npm run build && serve dist -l 3000",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "@tailwindcss/cli": "^4.1.3",
        "daisyui": "^5.0.17",
        "prettier": "^3.2.4",
        "prettier-plugin-tailwindcss": "^0.6.11",
        "serve": "^14.2.6",
        "tailwindcss": "^4.1.3"
    }
}
